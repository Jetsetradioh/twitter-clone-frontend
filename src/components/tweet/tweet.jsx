import React from "react";

const Tweet = () => {
  const submitHandler = () => {};

  return (
    <div>
      <form onSubmit={submitHandler}>
        <input type="text" />
      </form>
    </div>
  );
};

export default Tweet;
